# Give each Dataset Family its own Coverage declaration

Each Dataset Family declares coverage in coordinates matching its own grain and information-time semantics, while the Generation root preserves those distinct declarations. Financial Coverage records discovery progress, gaps, pending instruments, and reconciliation limits, allowing explicit degraded readiness without pretending sparse facts are incomplete Market Coverage.
