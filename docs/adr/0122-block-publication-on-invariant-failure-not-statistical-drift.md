# Block publication on invariant failure, not statistical drift

Deterministic correctness-invariant failures block immutable publication and leave the prior authoritative pointer unchanged. Statistical drift produces diagnostics rather than rejection because changed market behavior is not proof of an invalid result.
