# Keep one current Alpha Language with hard cuts

ThesisTrace admits Formulae against one current Alpha Language and freezes the resulting canonical expression. Renames, removals, or result-changing semantics are hard cuts: obsolete Formulae are rejected and only the current semantics execute.
