# Delete only terminal Research without cascading Tracks

Only a terminal ResearchRun may be permanently deleted; Delete never doubles as Cancel. Deletion removes unreferenced Run-owned state and artifacts but never mutates an independently seeded DailyTrack, whose copied Tracking Origin remains authoritative.
