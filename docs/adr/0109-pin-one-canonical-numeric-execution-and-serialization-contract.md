# Pin one canonical numeric execution and serialization contract

Every ResearchRun and DailyTrack binds the same explicit rules for integer counts, finite decimal accounting, binary64 calculation boundaries, missing values, and canonical serialization. The runtime never relies on language defaults or dispatches across historical numeric contracts.
