# Use a small static Alpha value model

Alpha expressions use a closed static model of Numeric Series, scalar Number, and bounded Window arguments, with explicit broadcast and complete Builtin signatures. Admission rejects mismatched types and non-series roots rather than introducing dynamic, categorical, tabular, or arbitrary Python values without a concrete research requirement.
