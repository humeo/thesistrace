# Keep Canonical Field definitions immutable

A Canonical Field has a stable identity and immutable meaning, type, unit, grain, and availability semantics. A meaning change creates a new Field identity, while corrected values belong to a later Data Generation and never reinterpret accepted Research input.
