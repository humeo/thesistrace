# Use one minimal Instrument Identity across asset families

Dataset Families share a deterministic Instrument Identity containing only stable identity and lifecycle coordinates, while asset-specific facts remain in their owning families. The current identity namespace is Tushare-backed ordinary A-shares and does not pre-model unsupported assets or a multi-provider registry.
