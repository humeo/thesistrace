# Evaluate Alpha as a Series execution plan

The Worker derives one transient deterministic post-order plan from the frozen Alpha Expression and computes each complete series node once per bounded execution slice. Research and Daily Tracking share those evaluators; the plan is neither persisted nor expanded into a second bytecode or execution truth.
