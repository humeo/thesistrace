# Grant one Operator capability without general RBAC

One active Researcher holds the indivisible Operator Capability, with initial assignment and transfer restricted to private administration. The Operator Console manages invitations, session revocation, and separate data-refresh requests, with each mutation requiring a current-password proof bound to that exact action. This provides limited product administration without a second administrator identity, general role hierarchy, or unrestricted access to another Researcher's research.
