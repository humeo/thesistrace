# Delete only stopped DailyTracks

A DailyTrack may be permanently deleted only after irreversible Stop reaches terminal `stopped`; Delete never stops or races active work. Research and DailyTrack deletion remain independent and remove physical objects only after durable references are gone.
