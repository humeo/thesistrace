# Require canonical exact Batch-Incremental Equivalence

Reference Research execution and session-by-session Daily Tracking must produce canonically exact results from the same origin, contracts, sessions, and inputs. Explicit verification may inspect transient values, but ordinary Advances never replay full reference history.
