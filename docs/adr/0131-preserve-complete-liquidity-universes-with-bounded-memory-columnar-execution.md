# Preserve complete Liquidity Universes with bounded-memory columnar execution

Research must evaluate every supported Liquidity Universe without truncation or sampling, including the largest current Top 3000 scope. ThesisTrace therefore uses bounded-memory columnar execution and accepts longer work rather than changing research semantics to fit memory.
