# Use the selected Liquidity Universe as the Strategy Benchmark

A Strategy Backtest compares against the equal-weight return of the same point-in-time Liquidity Universe frozen by its ResearchRun, using the same next-open holding intervals. This avoids an unrelated external-index dependency and makes the benchmark match the opportunity set actually researched.
