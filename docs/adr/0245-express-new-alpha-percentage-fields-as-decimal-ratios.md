# Express new Alpha percentage fields as decimal ratios

New Alpha-authorable percentage fields use decimal ratios, so 15% is represented as 0.15 and combines directly with ratios computed by a Formula; the interface may format the value as a percentage. Conversion requires verified source units for each Field and preserves raw source evidence, while multiples, amounts and existing Canonical Field meanings remain unchanged.
