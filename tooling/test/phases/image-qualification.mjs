import assert from 'node:assert/strict';
import { mkdirSync, readFileSync, writeFileSync, appendFileSync } from 'node:fs';
import { captureEvidence, captureLogs, scanCanaries } from '../evidence.mjs';

export async function imageQualification(run) {
  // The mounted production_mcp_image_api.py harness owns this test-only identity.
  const mcpResource = 'https://core.test/mcp';
  run.smoke_state = "/smoke-evidence/image-smoke-state.json";
  run.bootstrap_replay = "/smoke-evidence/tushare-financial-product-full-replay.json";
  await run.phase("image-smoke-images", () => run.buildImages());
  run.backend_image = run.project_name + "-initialize";
  run.image_revision = (await run.exec("docker", ["image", "inspect", run.backend_image, "--format", "{{.Id}}"], {capture: true})).trim();
  await run.phase("image-smoke-fresh-named-volume-mount", () => run.composeRun(["--entrypoint", "/bin/true", "batch-research-worker"], {base: true}));
  await run.phase("image-smoke-worker-capacity-rejection", () => run.check("verify_worker_capacity_rejection", []));
  await run.phase("image-smoke-research-child-cgroup-oom", () => run.check("verify_research_child_cgroup_oom_detection", []));
  await run.phase("image-smoke-infrastructure", () => run.compose(["up", "--detach", "--no-build", "--wait", "--wait-timeout", "300", "postgres", "rustfs"]));
  await run.phase("image-smoke-rustfs-s3-ready", () => run.waitForS3(true));
  await run.phase("image-smoke-initializers", () => run.compose(["up", "--detach", "--no-build", "initialize", "auth-initialize"]));
  await run.phase("image-smoke-initialization", () => run.compose(["wait", "initialize", "auth-initialize"]));
  await run.phase("image-publication-maintenance-full-page", () => run.composeRun([
    "publication-maintenance-worker", "python", "-c",
    readFileSync(`${run.repo_root}/tests/production_publication_maintenance.py`, 'utf8'),
  ], {stdoutFile: `${run.evidence_dir}/publication-full-page.json`, stderrFile: `${run.evidence_dir}/publication-full-page.stderr.log`}));
  await run.phase("image-smoke-bootstrap-replay", () => run.composeRun(["initialize", "python", "/smoke/tests/e2e/support/prepare_image_smoke_bootstrap_replay.py", "--input", "/smoke/tests/fixtures/tushare-financial-product-replay.json", "--output", run.bootstrap_replay]));
  await run.phase("image-smoke-operator-bootstrap", () => run.composeRun(["initialize", "thesistrace-data-operator", "bootstrap", "--idempotency-key", "financial-release-bootstrap", "--as-of", "2026-08-05T18:00:00+08:00", "--start-date", "2010-01-04", "--replay", run.bootstrap_replay]));
  await run.phase("image-smoke-prepare-data", () => run.composeRun(["initialize", "python", "/smoke/tests/e2e/support/prepare_image_smoke_data.py"]));
  await run.phase("image-smoke-mcp-preflight", () => run.check("run_mcp_image_smoke", ["preflight"], {stdoutFile: run.evidence_dir + "/mcp-preflight.json", stderrFile: run.evidence_dir + "/mcp-preflight.stderr.log"}));
  await run.phase("image-smoke-application", () => run.compose(["up", "--detach", "--no-build", "--wait", "--wait-timeout", "300", "auth", "agent", "api", "web"]));
  run.postgres_port = await run.mappedPort("postgres", "5432");
  run.s3_port = await run.mappedPort("rustfs", "9000");
  await run.phase("image-smoke-auth-session", () => run.provisionAuth());
  await run.phase("image-smoke-health", () => run.check("verify_image_health", ["initial"]));
  await run.phase("image-smoke-caddy-single-origin", () => run.check("verify_caddy_single_origin", [mcpResource]));
  await run.phase("image-smoke-caddy-backend-independence", () => run.check("verify_caddy_backend_independence", [mcpResource]));
  await run.phase("image-smoke-postgresql-readiness", () => run.check("verify_service_readiness_outage", ["postgres", "postgresql"]));
  await run.phase("image-smoke-rustfs-readiness", () => run.check("verify_service_readiness_outage", ["rustfs", "rustfs"]));
  await run.phase("image-smoke-auth-readiness", () => run.check("verify_service_readiness_outage", ["auth", "auth"]));
  await run.phase("image-smoke-dataset-readiness", () => run.check("verify_dataset_readiness_outage", []));
  await run.phase("image-smoke-operator-fixture", () => run.host(["uv", "run", "python", "apps/core/tests/acceptance/qualify_operator_image_smoke.py", "provision", run.operator_sessions_file], {stdoutFile: run.evidence_dir + "/operator-provision.json", stderrFile: run.evidence_dir + "/operator-provision.stderr.log"}));
  await run.phase("image-smoke-operator-worker-unavailable", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_OPERATOR_SESSION_FILE=/smoke-secrets/operator-sessions.json", "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "initialize", "python", "/smoke/tests/production_image_smoke.py", "operator-unavailable"], {stdoutFile: run.evidence_dir + "/operator-unavailable.json", stderrFile: run.evidence_dir + "/operator-unavailable.stderr.log"}));
  await run.phase("image-smoke-data-operator-secret-rejection", () => run.check("verify_data_operator_worker_secret_rejection", []));
  await run.phase("image-smoke-workers", () => run.compose(["up", "--detach", "--no-build", "--wait", "--wait-timeout", "120", "research-worker", "batch-research-worker", "tracking-worker", "publication-maintenance-worker", "data-operator-worker"]));
  await run.phase("image-smoke-single-data-operator-worker", () => run.check("verify_single_data_operator_worker", []));
  await run.phase("image-smoke-tushare-secret-scope", () => run.check("verify_tushare_secret_scope", []));
  await run.phase("image-smoke-operator-processed", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_OPERATOR_SESSION_FILE=/smoke-secrets/operator-sessions.json", "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "initialize", "python", "/smoke/tests/production_image_smoke.py", "operator-processed"], {stdoutFile: run.evidence_dir + "/operator-processed.json", stderrFile: run.evidence_dir + "/operator-processed.stderr.log"}));
  await run.phase("image-smoke-operator-transfer", () => run.host(["uv", "run", "python", "apps/core/tests/acceptance/qualify_operator_image_smoke.py", "transfer", run.operator_sessions_file], {stdoutFile: run.evidence_dir + "/operator-transfer.json", stderrFile: run.evidence_dir + "/operator-transfer.stderr.log"}));
  await run.phase("image-smoke-operator-transferred", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_OPERATOR_SESSION_FILE=/smoke-secrets/operator-sessions.json", "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "initialize", "python", "/smoke/tests/production_image_smoke.py", "operator-transferred"], {stdoutFile: run.evidence_dir + "/operator-transferred.json", stderrFile: run.evidence_dir + "/operator-transferred.stderr.log"}));
  await run.phase("image-smoke-receipt-age", () => run.composeRun(["initialize", "python", "/smoke/apps/core/tests/acceptance/age_operator_image_smoke_receipt.py", "image-smoke-operator-market-refresh"], {stdoutFile: run.evidence_dir + "/operator-receipt-aged.json", stderrFile: run.evidence_dir + "/operator-receipt-aged.stderr.log"}));
  await run.phase("image-smoke-receipt-collection", () => run.composeRun(["initialize", "thesistrace-data-operator", "collect", "--idempotency-key", "image-smoke-operator-receipt-retention"], {stdoutFile: run.evidence_dir + "/operator-receipt-collection.json", stderrFile: run.evidence_dir + "/operator-receipt-collection.stderr.log"}));
  await run.phase("image-smoke-receipt-collection-count", () => run.exec("grep", ["-F", "\"deleted_receipt_count\":1", run.evidence_dir + "/operator-receipt-collection.json"]));
  await run.phase("image-smoke-operator-receipt-cleaned", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_OPERATOR_SESSION_FILE=/smoke-secrets/operator-sessions.json", "-e", "THESISTRACE_TEST_OPERATOR_STATE=/smoke-evidence/operator-processed.json", "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "initialize", "python", "/smoke/tests/production_image_smoke.py", "operator-receipt-cleaned"], {stdoutFile: run.evidence_dir + "/operator-receipt-cleaned.json", stderrFile: run.evidence_dir + "/operator-receipt-cleaned.stderr.log"}));
  await run.phase("image-smoke-receipt-worker-stop", () => run.compose(["stop", "data-operator-worker"]));
  await run.phase("image-smoke-receipt-worker-reopen", () => run.compose(["up", "--detach", "--no-build", "--wait", "--wait-timeout", "120", "data-operator-worker"]));
  // The recovery matrix submits more than the ordinary daily Run quota. Operator
  // authorization/revocation was checked above; use its current owner for research.
  await run.phase("image-smoke-research-operator-session", async () => {
    const sessions = JSON.parse(readFileSync(run.operator_sessions_file, 'utf8'));
    writeFileSync(run.auth_session_file, JSON.stringify(sessions.successor_operator), { mode: 0o600 });
  });
  run.network_internal = (await run.exec("docker", ["network", "inspect", run.project_name + "_default", "--format", "{{.Internal}}"], {capture: true})).trim();
  assert.equal(run.network_internal, "true");
  writeFileSync(run.evidence_dir + "/network.txt", "internal=" + run.network_internal + "\n");
  await run.compose(["images", "--format", "json"], {stdoutFile: run.evidence_dir + "/images.jsonl"});
  await run.compose(["ps", "--all"], {stdoutFile: run.evidence_dir + "/compose-ps-before-restart.txt"});
  await run.phase("image-smoke-before", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "-e", "THESISTRACE_TEST_IMAGE_ID=" + run.image_revision, "initialize", "python", "/smoke/tests/production_image_smoke.py", "before"], {stdoutFile: run.evidence_dir + "/smoke-before.json", stderrFile: run.evidence_dir + "/smoke-before.stderr.log"}));
  await run.phase("image-smoke-worker-loss", () => run.compose(["kill", "research-worker"]));
  await run.phase("image-smoke-expire-worker-loss", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "initialize", "python", "/smoke/tests/production_image_smoke.py", "expire-worker-loss"], {stdoutFile: run.evidence_dir + "/smoke-expire-worker-loss.json", stderrFile: run.evidence_dir + "/smoke-expire-worker-loss.stderr.log"}));
  await run.phase("image-smoke-checkpointed-application-restart", () => run.compose(["restart", "api", "tracking-worker"]));
  await run.phase("image-smoke-checkpointed-database-restart", () => run.compose(["restart", "postgres"]));
  await run.phase("image-smoke-checkpointed-post-database-restart", () => run.compose(["restart", "api", "tracking-worker"]));
  await run.phase("image-smoke-checkpointed-reopen", () => run.compose(["up", "--detach", "--no-build", "--wait", "--wait-timeout", "120", "api", "tracking-worker"]));
  await run.phase("image-smoke-checkpointed", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "initialize", "python", "/smoke/tests/production_image_smoke.py", "checkpointed"], {stdoutFile: run.evidence_dir + "/smoke-checkpointed.json", stderrFile: run.evidence_dir + "/smoke-checkpointed.stderr.log"}));
  await run.phase("image-smoke-worker-reopen", () => run.compose(["up", "--detach", "--no-build", "--wait", "--wait-timeout", "120", "research-worker"]));
  await run.phase("image-smoke-recovered", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "initialize", "python", "/smoke/tests/production_image_smoke.py", "recovered"], {stdoutFile: run.evidence_dir + "/smoke-recovered.json", stderrFile: run.evidence_dir + "/smoke-recovered.stderr.log"}));
  await run.phase("image-smoke-application-restart", () => run.compose(["restart", "api", "research-worker", "batch-research-worker", "tracking-worker", "publication-maintenance-worker", "data-operator-worker"]));
  await run.phase("image-smoke-database-restart", () => run.compose(["restart", "postgres"]));
  await run.phase("image-smoke-post-database-restart", () => run.compose(["restart", "api", "research-worker", "batch-research-worker", "tracking-worker", "publication-maintenance-worker", "data-operator-worker"]));
  await run.phase("image-smoke-reopen", () => run.compose(["up", "--detach", "--no-build", "--wait", "--wait-timeout", "120", "api", "research-worker", "batch-research-worker", "tracking-worker", "publication-maintenance-worker", "data-operator-worker"]));
  await run.phase("image-smoke-persisted", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "initialize", "python", "/smoke/tests/production_image_smoke.py", "persisted"], {stdoutFile: run.evidence_dir + "/smoke-persisted.json", stderrFile: run.evidence_dir + "/smoke-persisted.stderr.log"}));
  await run.phase("image-smoke-rustfs-outage", () => run.compose(["stop", "rustfs"]));
  await run.phase("image-smoke-publish-transient-target", () => run.composeRun(["initialize", "python", "/smoke/tests/e2e/support/prepare_image_smoke_data.py", "lagged"], {stdoutFile: run.evidence_dir + "/transient-target.json", stderrFile: run.evidence_dir + "/transient-target.stderr.log"}));
  await run.phase("image-smoke-transient-failed", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "initialize", "python", "/smoke/tests/production_image_smoke.py", "transient-failed"], {stdoutFile: run.evidence_dir + "/smoke-transient-failed.json", stderrFile: run.evidence_dir + "/smoke-transient-failed.stderr.log"}));
  await run.phase("image-smoke-rustfs-reopen", () => run.compose(["up", "--detach", "--no-build", "--wait", "--wait-timeout", "120", "rustfs"]));
  await run.phase("image-smoke-after", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "initialize", "python", "/smoke/tests/production_image_smoke.py", "after"], {stdoutFile: run.evidence_dir + "/smoke-after.json", stderrFile: run.evidence_dir + "/smoke-after.stderr.log"}));
  await run.phase("image-smoke-reset-worker-stop", () => run.compose(["stop", "research-worker", "batch-research-worker"]));
  await run.phase("image-smoke-reset-ready", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "initialize", "python", "/smoke/tests/production_image_smoke.py", "reset-ready"], {stdoutFile: run.evidence_dir + "/smoke-reset-ready.json", stderrFile: run.evidence_dir + "/smoke-reset-ready.stderr.log"}));
  await run.phase("image-smoke-worker-logs", () => captureLogs(run, ["worker-events.jsonl"]));
  await run.phase("image-smoke-api-logs", () => captureLogs(run, ["api-events.jsonl"]));
  await run.phase("image-smoke-gateway-auth-logs", () => captureLogs(run, ["caddy-events.jsonl", "auth-events.jsonl"]));
  await run.phase("image-smoke-worker-event-contract", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "-e", "THESISTRACE_TEST_WORKER_EVENTS=/smoke-evidence/worker-events.jsonl", "initialize", "python", "/smoke/tests/production_image_smoke.py", "worker-events"], {stdoutFile: run.evidence_dir + "/smoke-worker-events.json", stderrFile: run.evidence_dir + "/smoke-worker-events.stderr.log"}));
  await run.phase("image-smoke-product-state-reset", () => run.resetProductState());
  await run.phase("image-smoke-reset-api", () => run.compose(["up", "--detach", "--no-build", "--wait", "--wait-timeout", "120", "auth", "api"]));
  await run.phase("image-smoke-reset-auth-session", () => run.provisionAuth());
  await run.phase("image-smoke-reset-verified", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "initialize", "python", "/smoke/tests/production_image_smoke.py", "reset"], {stdoutFile: run.evidence_dir + "/smoke-reset.json", stderrFile: run.evidence_dir + "/smoke-reset.stderr.log"}));
  await run.compose(["ps", "--all"], {stdoutFile: run.evidence_dir + "/compose-ps-after-restart.txt"});
  await run.phase("image-smoke-bounded-evidence", () => captureEvidence(run));
  await run.phase("image-smoke-observability", () => run.composeRun(["-e", "THESISTRACE_TEST_API_ORIGIN=http://api:8100", "-e", "THESISTRACE_TEST_WEB_ORIGIN=http://web:" + run.caddy_port, "-e", "THESISTRACE_TEST_SMOKE_STATE=" + run.smoke_state, "-e", "THESISTRACE_TEST_API_EVENTS=/smoke-evidence/api-events.jsonl", "-e", "THESISTRACE_TEST_WORKER_EVENTS=/smoke-evidence/worker-events.jsonl", "-e", "THESISTRACE_TEST_CADDY_EVENTS=/smoke-evidence/caddy-events.jsonl", "-e", "THESISTRACE_TEST_AUTH_EVENTS=/smoke-evidence/auth-events.jsonl", "initialize", "python", "/smoke/tests/production_image_smoke.py", "observability"], {stdoutFile: run.evidence_dir + "/smoke-observability.json", stderrFile: run.evidence_dir + "/smoke-observability.stderr.log"}));
  await run.phase("image-smoke-mcp-http-before", () => run.check("run_mcp_image_smoke", ["http-before"], {stdoutFile: run.evidence_dir + "/mcp-http-before.json", stderrFile: run.evidence_dir + "/mcp-http-before.stderr.log"}));
  await run.phase("image-smoke-mcp-api-stop", () => run.compose(["stop", "--timeout", "30", "api"]));
  await run.phase("image-smoke-mcp-api-stopped", () => run.check("capture_stopped_api_state", []));
  await run.phase("image-smoke-mcp-api-reopen", () => run.compose(["up", "--detach", "--no-build", "--wait", "--wait-timeout", "120", "api"]));
  await run.phase("image-smoke-mcp-workers", () => run.compose(["up", "--detach", "--no-build", "--wait", "--wait-timeout", "120", "research-worker", "batch-research-worker", "tracking-worker", "publication-maintenance-worker", "data-operator-worker"]));
  await run.phase("image-smoke-mcp-http-after", () => run.check("run_mcp_image_smoke", ["http-after"], {stdoutFile: run.evidence_dir + "/mcp-http-after.json", stderrFile: run.evidence_dir + "/mcp-http-after.stderr.log"}));
  await run.phase("image-smoke-mcp-stdio", () => run.check("run_mcp_image_smoke", ["stdio"], {stdoutFile: run.evidence_dir + "/mcp-stdio.json", stderrFile: run.evidence_dir + "/mcp-stdio.stderr.log"}));
  await run.phase("image-smoke-mcp-api-logs", () => captureLogs(run, ["api-events.jsonl"]));
  await run.phase("image-smoke-mcp-evidence", () => run.check("run_mcp_image_smoke", ["evidence"], {stdoutFile: run.evidence_dir + "/mcp-image-evidence.json", stderrFile: run.evidence_dir + "/mcp-image-evidence.stderr.log"}));
  run.operator_browser_state_root = run.run_root + "/operator-browser-state";
  run.data_mount = run.operator_browser_state_root + "/canonical-data";
  run.benchmark_mount = run.operator_browser_state_root + "/benchmark-data";
  run.batch_attempt_control_directory = run.operator_browser_state_root + "/batch-attempt-control/.batch-attempts";
  for (const path of [run.data_mount, run.benchmark_mount, run.batch_attempt_control_directory]) mkdirSync(path, {recursive: true});
  appendFileSync(run.run_metadata, "operator_browser_state_root=" + run.operator_browser_state_root + "\n");
  await run.phase("image-smoke-operator-browser-reset", () => run.resetProductState());
  await run.phase("image-smoke-operator-browser-bootstrap", () => run.composeRun(["initialize", "thesistrace-data-operator", "bootstrap", "--idempotency-key", "financial-release-bootstrap", "--as-of", "2026-08-05T18:00:00+08:00", "--start-date", "2010-01-04", "--replay", "/smoke/tests/fixtures/tushare-financial-product-replay.json"]));
  await run.phase("image-smoke-operator-browser-prepare-data", () => run.composeRun(["initialize", "python", "/smoke/tests/e2e/support/prepare_current_data.py"]));
  await run.phase("image-smoke-operator-browser-worker", () => run.compose(["up", "--detach", "--no-build", "--wait", "--wait-timeout", "120", "data-operator-worker"]));
  await run.phase("image-smoke-operator-browser-market-baseline", () => run.composeRun(["initialize", "python", "/smoke/tests/e2e/support/publish_financial_track_head.py", "lagged"]));
  await run.phase("image-smoke-operator-browser-financial-baseline", () => run.composeRun(["initialize", "python", "/smoke/tests/e2e/support/publish_financial_track_head.py", "recovered"]));
  await run.phase("image-smoke-operator-browser-application", () => run.compose(["up", "--detach", "--no-build", "--wait", "--wait-timeout", "300", "auth", "auth-fixture-control", "api", "research-worker", "batch-research-worker", "tracking-worker", "publication-maintenance-worker", "data-operator-worker", "web"]));
  run.auth_fixture_port = await run.mappedPort("auth-fixture-control", "8260");
  run.auth_fixture_origin = "http://127.0.0.1:" + run.auth_fixture_port;
  await run.phase("image-smoke-operator-browser-auth-fixture-ready", () => run.waitForAuthFixture());
  await run.phase("image-smoke-operator-browser", () => run.host(["pnpm", "exec", "playwright", "test", "--config", "tests/playwright.config.ts", "operator-console.spec.ts", "--grep", "Operator Market submission and response recovery"]));
  await run.phase("image-smoke-final-evidence", () => captureEvidence(run));
  await run.phase("image-smoke-final-secret-scan", () => scanCanaries(run));
}
