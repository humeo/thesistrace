# 03 — 末日正常交易，追踪只追加新日期

**What to build:** 成功回测最后一天完成应执行的交易，DailyTrack 从该完整账户继续，已发布的净值和决策不会因刷新或缓存丢失而改变。

**Blocked by:** 01 — 让信号评估与策略执行真正分开

**Status:** complete

- [x] 首个 Research Open 保持全现金；最后纳入的 Session 正常执行前日到期决定、扣费后记录 Open NAV，不因结束强平。
- [x] 最终 Close 的下一 Open 决定以及决定所需的实际名单、当前相对权重、数据/合同身份进入权威终端状态，不只保存 Signal 日期；本票以现有等权、满仓规则证明。
- [x] 同一状态转换用于普通 Run、Batch 和 DailyTrack；后续 Advance 只增加更晚 Sessions，不替换旧终点，不重置选股相位、初始本金或累计峰值/回撤。
- [x] 工作缓存丢失后仍使用已冻结的过去目标；本次新 Data Generation 可影响未来决定，但不能重算已决定的上一期目标。
- [x] 在恰好有交易、费用和最后 Close 新决定的多个切点，独立整段计算与分段推进的每日账户及终态一致；替代旧边界覆盖断言并保留独立峰值/回撤覆盖。
- [x] 真实依赖验证并发 Refresh、Worker 中断/Retry、原子 Checkpoint 发布；失败仍读上个完整状态，重复请求不重复推进，手动 Refresh/无新 Session 反馈保持准确。
- [x] 当前末日及 Track 状态只使用完整已执行账户，不增加旧终点转换或多版本续算。
- [x] 源 Run 删除后 Track 自有 Origin 仍足够续算，接口与页面明确完成日期和交易时点。

## Notes

规格：末日交易、固定 Origin、权威目标、严格追加与当前 Track 状态。

母规格：Agent Quant Research：信号评估、策略回测与前向追踪。遵循其当前合同与范围；每票直接交付当前合同，前端、MCP、验证随行为交付，不作为尾部补接工作。

## Comments

- 2026-09-12 最新执行约束：用户明确当前为开发阶段。本轮只实现单一当前合同，直接修改调用方与产物定义，不新增兼容分支、字段别名、旧合同读取适配、vXX 升级或版本迁移。先前所有存量转换、迁移备份/回滚/重复升级验收均撤回；其余产品功能和失败恢复验收保持。只操作新 worktree 与隔离测试资源，不删除或重置现有 dev/线上数据。严格按 01–14 串行：每票计划 → 实现 → 验证 → Standards 审查 → Spec 审查 → 修复复审 → tracker 更新 → 独立提交。

- 2026-09-12：用户确认 14 票拆分及阻塞关系，正式发布为 ready-for-agent；尚未开始实现。

- 2026-09-12：完成末日交易、冻结下一 Open 目标、单一完整终态及严格追加 Track。跨接口 405 项、终态/Chunk 24 项、恢复往返 13 项、结果 codec 26 项、网页 24 项通过；真实本金/单 Session 首次入场、冷缓存修订隔离、并发与恢复、六项依赖重启、1000 次追加读成本、发布字节/Session 坐标 26 项及工作台 Run→Track E2E 通过。首次失败与修正证据见实施计划。Standards P2 等权目标校验已修复并复审关闭；Spec 无阻塞发现；Ruff、类型检查与 diff check 通过。本条与实现共同独立提交。
