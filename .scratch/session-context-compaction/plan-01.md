# Issue 01 实现计划

状态：本票实现、验证及复审完成，随本票独立提交。隔离分支 codex/session-context-compaction；基线 3d75080 是原工作区既有改动的独立快照，不是本功能实现提交。

严格串行：本票验收、Standards 审查、Spec 审查、修复复审、tracker 更新及独立提交完成前，不开始 02。用户的串行要求优先于 review 技能的并行建议。

1. 先通过公开配置与 Provider 测试建立失败回归：必填模型输出能力/压缩门槛、完整输入动态额度、合法长输出、明确 length 停止、小窗口拒绝与工具执行屏障。
2. 抽取实际 Provider 消息 tokenx 计数和请求预算；保留合法协议数据，仅排除内部重复元数据。模型 registry 硬切换必填字段，同步所有当前 Fixture、Scripted/Eval 和配置读取方。
3. 输出 D 由配置和实际输入共同决定；去除 8192 与累计生成 256 KiB 限制。结构化停止信息保持可由后续控制器处理，不预先锁死 Run 终态；保留超时、取消、usage 和工具传输保护。
4. 接通模型选择、低于 K 禁用原生辅助压缩、Provider 到聊天的明确错误投影；保留部分正文和成功操作回执。验证 length 响应即使携带完整工具调用也不能释放操作。
5. 跑受影响 Agent/合同/Web 测试及 typecheck；实际 HTTP payload 使用本地 Provider Fixture，业务执行屏障以 Scripted 模型和必要的隔离 PostgreSQL 验证。不启动或清理 thesistrace-dev。
6. 捕获本票相对基线的代码和验收证据，依次做 Standards 和 Spec 审查，修复后重验/复审；全部满足才更新 01 勾选项并独立提交。

首次正常压缩属于 03，自动重新生成属于 05，本票不提前实施。整体验收与真实模型质量评估仍由 06 把关。
