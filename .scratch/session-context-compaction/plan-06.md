# Issue 06 实施计划：最终集成验收与交付

状态：complete。实现、验证、审查和独立提交完成。基线 `94d412e`，01—05 已独立提交；仅在隔离 worktree `codex/session-context-compaction` 串行执行。

1. 核对前五票证据与当前公开契约，建立需求到测试/实际 Provider payload/数据库证据的映射。缺口按所属功能归因修复，保留失败证据，不以最终验收掩盖未完功能。
2. 使用现有隔离 Core/MCP、Agent 和浏览器入口串联分页、规范化工具结果、最后/多工具结果后的压缩、恢复和持久化展示。复用确定性 Fixture，覆盖小窗口、模型切换、稳定前缀和一次恢复约束。
3. 审计旧路径及安全观测数据，更新运行说明、模型配置、工具契约、会话生命周期、错误和 Mastra patch 文档。记录结构性前缀稳定；真实缓存用量只报告实际 usage。
4. 为首次、增量和长轮次压缩运行固定研究案例的独立摘要质量评估；使用当前生产候选生成入口、选定模型与 effort，报告事实/游标/错误保留、调用 usage、成本与耗时。工程测试不访问真实模型，评估不接入开发数据；保留未验证项，不将缺失评估算通过。
5. 在最终版本执行 `pnpm check` 和 `pnpm test:image-smoke`，使用仓库隔离依赖。定位和修复失败，记录命令与退出结果，仅按实际改动复验。
6. Standards → Spec 串行 review，修复后复审；更新 tracker、证据及独立提交。只有全部必需验收完成才标记 06、父 spec 和 goal 完成。

约束：不增加兼容、迁移、fallback 或中间版本路径；不修改原 main checkout，不重置/删除 thesistrace-dev 容器、卷或 Dataset Head；只清理本次隔离资源。每次模型调用遵循共享预算/超时/取消控制，日志不写原文或 M/S。

## 首轮审计补充

当前诊断只发Run/Tool生命周期事件，并在Run末取最后预算与压缩统计。补齐验收要求时采用只读逐调用事件（预算、usage、耗时、缓存计数；未知保持未知）、Tool完成字节数、恢复领取次数。通过现有Guard/运行诊断边界传递闭合元数据，不缓存正文或新建可重放日志；sink失败不得改变业务结果。修改前补相应安全/行为断言，完成后复验受影响Agent测试。

独立质量探针使用生产generateSessionContext和固定合成来源，保存在本目录quality-eval.mjs。先零调用preflight，后Luna/high有限费用预留的真实调用；保留首轮失败，不通过挑成功例或无限重试掩盖失败。该探针本身不接触Core数据，后续结合真实Core/MCP浏览器工程闭环评估续接。

## 压缩浏览器闭环的确定性接入

现有全产品E2E使用Scripted Provider；生产候选生成已由真实模型与Fake/PG证明，但Scripted尚无M/S响应。为浏览器补齐小型、明确标记的Scripted压缩场景，仍走生产控制器/PG/Provider Guard：辅助调用返回合法固定候选；主调用通过明确测试提示覆盖普通压缩与一次length恢复/再次失败。使用真实Core get_research_context等现有读取工具，不伪造HTTP Agent时间线或跳过持久化。

浏览器测试在独立Test Session中预置固定原始历史（通过既有隔离PG测试入口），再由UI提交下一条请求；核对检查点revision、原文保留、内部消息隐藏、独立替代正文及刷新后一致性。先建立失败回归，按已有Scripted模块模式增加受控响应，不增加生产模型fallback、独立压缩调度或共享记忆。恢复中展示已有生产组件fixture证据须与完整闭环区分；如果可在完整链路采用现有可控屏障，则追加该状态的现场验收。
