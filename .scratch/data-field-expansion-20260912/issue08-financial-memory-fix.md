# Issue 08 — 财务发布完整验证的内存修复

状态：修复已部署，原任务已恢复；正常财务刷新终态和行业刷新仍待验收。

## 问题与范围

2026-09-14 07:02:12 UTC，生产主机的 global OOM 杀死 Data Operator Worker。随后租约仍有效，自动重启退出；已停止该 Worker，其他服务继续运行。已发布 Head 保持 c8596320，新指标候选与三表候选均保留。禁止删除数据、手工修改任务状态或绕过发布验证。

本次修复只处理 FinancialCandidateStore.validate_stored 的全量 Python 内存放大，不修改数据契约、财务字段、TTM、可用日期或发布 CAS。

## Plan

1. 用现有候选构建可重复的内存失败回归，并对真实全量候选设置进程内存预算。
2. 按证券投影来源版本，只保留来源事实摘要；逐对象验证落盘表，保留增量中后写版本的优先级。
3. 验证来源事实、可用日期、Parquet 字节、行数、排序、分区与历史候选完整性。
4. 完成 Standards → Spec 串行审查、独立提交、main 定向复验后，按 main → 部署分支发布。
5. 恢复已有生产财务刷新，验证 Head 与保留 family，再继续行业刷新验收。

## 复现与验证

- 公开 validate_stored 回归：6,000 条额外版本，修复前 Python 峰值 28,823,459 字节，超过 16 MiB 预算；1 failed / 65 deselected，15.11 秒。
- 修复后同一回归通过；财务候选模块 69 passed，34.97 秒，包括多次增量追加及重新签名后的来源值、可用日期、重复分区损坏拒绝。
- 数据集组合与指标家族模块：75 passed，20.38 秒。
- Ruff 与 git diff --check 通过。
- 真实全量旧候选 a1a8b35e（1,242,613 条物理行）：修复前复现进程在 366.78 秒、峰值 1,147.88 MiB 时主动退出 42，完整来源版本投影结束后，在构建宽字段来源副本时超过预算。设置预算为 1,024 MiB，采样间隔 1 秒；未使本机发生 OOM。
- 隔离 PostgreSQL 定向发布测试：20260914t072214z-46617-f31b1913，6 passed / 59 deselected，12.17 秒，清理成功。
- 修复后同一完整候选验证通过：退出 0，1,010.43 秒，进程峰值 688.69 MiB，低于 1,024 MiB 预算。完整源事实和全部存储对象检查均执行。

## 实现

来源版本按证券迭代，跨证券仅保存来源事实 SHA-256 摘要。落盘对象逐个核验完整字节与 schema，用紧凑源行标识实现增量最后写入优先；排序和分区边界保留紧凑元数据并在完整遍历后核验。读取完整表的现有接口仍返回排序后的行，但发布验证不再调用完整表物化。

没有跳过完整验证，没有添加运行时兼容路径，没有更改已发布数据。

## 独立审查

冻结快照 /tmp/issue08-financial-memory-review-b_lny5k2；Standards → Spec 严格串行，两项均 0 个新增可操作发现。审查未把全量和生产验证视为已经完成。

## 集成

独立提交 7b3ce5b6e7f97ef60616667269fbdf2a56b5c424 已快进 main。原有 tracked dirty patch 与 1,868 个 untracked 文件名逐字节保留。main 的隔离 PostgreSQL 复验 20260914t073742z-49563-65aa3402：6 passed，13.17 秒，清理成功；数据模块复验 144 passed，51.20 秒。

## 原因与防止再次发生

原有有界内存回归覆盖投影读取和另一条 validate 路径，未覆盖最终发布调用的 validate_stored。后者同时保留完整 CanonicalFinancialVersion、宽字段字典及整表 Python 行，随全市场历史规模增长。本次把内存预算回归放在实际公开验证入口，并补全重新签名后仍拒绝语义损坏及多层增量重开证据；完整候选资源验证也保留结果。没有引入新的验证框架或可选择后端。

## 生产恢复

服务器构建与启动成功，配置未变。原任务按既有机制恢复为 attempt 2，最近 heartbeat age 7 秒、内存 621.8 MiB；候选继续保留，组合 generation 尚未产生。没有新建替代任务，也没有修改数据库任务状态。三表另有 4 个 canonical_projection 校验失败标的，原数据保留；其限制与 Worker 内存故障分开记录。

终态补充：原任务 attempt 2 已 succeeded，实际财务 Head cd5d70e0 发布成功；其后的正常行业刷新也 succeeded，最终 Head 4c334073 保留全部 13 家族。恢复后的最新运行采样约 490 MiB，未再发生已观察到的 OOM。生产成功回执和最终浏览器核验见 [生产发布记录](issue08-production-release.md)。原始总 elapsed 包含首次失败及暂停时间，不作为 attempt 2 独立耗时。
