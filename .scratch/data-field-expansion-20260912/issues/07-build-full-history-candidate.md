# 07 — 构建并验证 226 字段完整历史候选数据集

**What to build:** Data Operator 能复用已保留数据，补齐两个新来源和确证缺口，得到覆盖声明研究范围、可核查且可发布的 226 字段候选；验证候选发布与后续刷新不会丢掉并发更新或历史研究输入。

**Blocked by:** 02 — 接入 daily_basic 的 16 个 DSL 入口；03 — 开放三表的 16 个期末存量字段；04 — 提供 19 个 TTM 流量及不同期缺失规则；06 — 补齐 fina_indicator 全部 163 个指标

**Status:** ready-for-agent

**Execution contract:** 在从 main 创建并核实的新 worktree 中，严格按 01→08 串行执行。每票开始实现前先记录 Plan；完成实现后逐项验收，进行代码审查、修复及复审，更新 tracker 并形成该票独立 git commit，完成后才开始下一票。审查同样串行，不并行委派实现或审查。

**Latest user decision:** 按用户最新要求，直接实现当前合同，不做兼容、版本迁移或 vXX 升级链；这覆盖母规格中有条件引入迁移的旧提议。保留数据、原有字段语义与必要研究引用的要求继续有效，不将“不迁移”解释为允许清库。

**Execution order:** 07；必须先完成 06 的验收、复审、tracker 更新和独立提交。

- [ ] 确定候选明确的 Coverage 起点和本次结束日期，复用当前证券身份、日历、价格、复权、交易状态、股票池、行业和基准依赖，包括历史退市证券；不会把现存上市名单当完整历史 universe。
- [ ] 在准备环境中跑完两个新来源所需历史分片和增量尾部，保存每片请求范围、完成度、取全证据及可恢复检查点。6000/100 行上限、权限、限流、超时和空目标不能误记为完整。
- [ ] 从已保存三表重建新增 35 项投影与必要种子，仅补采有证据的原始记录缺口。核验覆盖起点及首年后续 TTM 目标的全部 pre-start 依赖，保留原年报/存量种子，不扩大 pre-2010 研究范围。
- [ ] 形成逐字段、年份和公司类型的来源资格及非空覆盖报告，区分采集未完成、来源实际缺失和不适用；保留历史回填证据限制。所有 226 项均满足各自可用性门槛，不能仅以字段存在或名称计数验收。
- [ ] 合成 equity.eod_price 7 项、equity.daily_basic 15 项、equity.financial_pit 41 项、equity.financial_indicator 163 项的完整候选；总数 226、TTM 19，原 12 项语义和值在相同输入上保持。无变化家族和分区复用引用，原始历史和被引用字节不原地改写。
- [ ] 完整候选可以独立重开并供隔离研究读取，财务按可见时间、每日指标按精确日期结合相同股票身份；混合来源公式完成计算，1 列与多列按实际依赖读取，研究不联网补取。
- [ ] 在真实隔离 Head、存储和 Worker 上验证发布：其他家族先推进 Head 时按最新根重组；目标家族已变化时重新构建或核验继承关系，不能只重挂陈旧候选覆盖新数据。
- [ ] CAS 前失败保持原 Head；CAS 已成功但完成回执失败时按发布坐标对账并收敛为已发布结果，不误报未发布或自动倒退 Head。重复执行不重复发布，过程和结果有恢复证据。
- [ ] 对候选发布后的行情、三表、指标和行业刷新逐项证明非目标家族、新字段、字段声明和真实覆盖持续保留；某一来源未完成不能被同组其他来源成功掩盖。
- [ ] 新代码可重试固定旧 Generation 的既有 Run，旧根明确拒绝新字段；新 Run 固定新 Head。Track 新 Attempt 固定当时验证的 Head，Origin、已有检查点和字段语义保留。准备、失败、清理期间被引用数据受到生命周期保护。
- [ ] 保存候选根身份、继承的源 Head、实际字段/覆盖摘要、资格账本和隔离验收结果，足以作为 08 的发布输入。候选可通过独立核查，但本票不切换生产 Head；若准备期间源 Head 推进，08 必须重新对齐并验证。

**Verification:** 母规格 T01、T02、T07、T08、T10、T11、T12、T13。真实来源资格和历史采集证据与确定性测试分开；并发、恢复、引用及 Worker 结论用现有隔离真实依赖验证，不操作共享开发数据。

## Comments

- 2026-09-12：用户确认发布工单。已纳入最新的串行执行、每票 Plan / 验收 / 审查修复复审 / tracker / 独立提交，以及不兼容、不做版本迁移的要求。
