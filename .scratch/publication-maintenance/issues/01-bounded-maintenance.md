# 01 有界 Publication 清理与持久化进度

Status: ready-for-agent

按 [方案](../spec.md) 实现单页发现、已处理键游标、单对象安全复查、有界明确删除、全局数据库协调及独立调度/退避状态。增加显式版本迁移及反向迁移，复用当前数据库基础设施。

验收：先建立多页/预算和并发失败回归；真实 PostgreSQL / 当前 RustFS 验证 StartAfter、断点与重启、并发引用、连接丢失、幂等及迁移的保留数据/失败回滚/重复执行。单页只查询该页 hash。此提交提供新维护接口，旧业务调用与旧接口在 02 同时移除，避免中间版本出现失效调用。定向检查通过后审查、修复并单独提交。

## Comments

2026-09-10：已实现单页扫描、持久预算、会话锁、逐对象复查、独立失败退避及显式双向迁移。
真实依赖：31 项 Publication/maintenance/migration 回归通过（run 20260910t075850z-48479-5afb762e）；
随后新增真实 SIGKILL 接管及删除响应丢失，maintenance/advisory-lock 19 项通过（run 20260910t080207z-57272-38b3e3df）。
两轴审查发现 HEAD 期间断连后继续删除、扫描完成与冷却分离两个窗口，均已修复并复审关闭。
Core 接口与独立入口一起提交以保持旧扫描接口移除后的可运行性；容器接入和镜像验收归入 02。

补充：六个真实空闲业务子进程零 LIST（run 20260910t080604z-60065-a7c31c58），镜像 smoke 修复轻量健康探针后通过（run 20260910t080304z-57624-8af4a710）。
