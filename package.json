{
  "name": "thesistrace",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=11 <12"
  },
  "scripts": {
    "bootstrap": "./scripts/dev-runtime bootstrap",
    "dev": "./scripts/dev-runtime watch",
    "dev:logs": "./scripts/dev-runtime logs",
    "dev:up": "./scripts/dev-runtime up",
    "dev:reset": "./scripts/dev-runtime reset",
    "dev:erase": "./scripts/dev-runtime erase",
    "dev:stop": "./scripts/dev-runtime stop",
    "test": "uv run ruff check src tests && uv run pytest -q -n 4 tests/kernel tests/architecture tests/adapters tests/data tests/entrypoints && pnpm --dir agent typecheck && pnpm --dir agent test && pnpm --dir auth typecheck && pnpm --dir auth test && pnpm --dir web typecheck && pnpm --dir web test:shell",
    "test:cleanup": "./scripts/test-runtime cleanup",
    "test:e2e": "./scripts/test-runtime e2e",
    "test:benchmark": "./scripts/test-runtime benchmark",
    "test:caddy-image-smoke": "./scripts/test-caddy-production-image",
    "test:image-smoke": "./scripts/test-runtime image-smoke && pnpm --dir auth test:image-smoke && pnpm --dir agent test:image-smoke && pnpm test:caddy-image-smoke",
    "test:integration": "./scripts/test-runtime integration && pnpm --dir auth test:integration && pnpm --dir agent test:integration",
    "test:codex-mcp": "./scripts/test-runtime codex-mcp",
    "check": "pnpm test && pnpm test:integration && pnpm test:e2e",
    "check:performance": "./scripts/test-runtime performance",
    "check:release": "pnpm check && pnpm test:image-smoke",
    "check:live-tushare": "uv run python scripts/check_live_tushare.py"
  }
}
